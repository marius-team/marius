[tool.black]
line-length = 120

[tool.isort]
profile = "black"
line_length = 120

[tool.pytest.ini_options]
pythonpath = [
    "."
]

[build-system]
requires = ["setuptools==44.0", "torch>=1.7.1"]
build-backend = "setuptools.build_meta"
