device_type: cpu
dataset: ???
edges:
  type: FLAT_FILE
  options:
    dtype: int
embeddings:
  type: HOST_MEMORY
  options:
    dtype: float
features:
  type: HOST_MEMORY
  options:
    dtype: float
save_model: true