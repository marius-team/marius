device_type: cpu
dataset: ???
edges:
  type: HOST_MEMORY
  options:
    dtype: int
embeddings:
  type: HOST_MEMORY
  options:
    dtype: float
features:
  type: HOST_MEMORY
  options:
    dtype: float